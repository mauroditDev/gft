CREATE TABLE IF NOT EXISTS prices ( price_id INTEGER PRIMARY KEY, brand_id INTEGER NOT NULL, start_date TIMESTAMP NOT NULL, end_date TIMESTAMP, product_id INTEGER NOT NULL, priority INTEGER DEFAULT 0 NOT NULL, price DECFLOAT(2) NOT NULL, curr VARCHAR(3) NOT NULL );